import { Command, flags } from '@heroku-cli/command';
export default class ConfigEdit extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        app: flags.IOptionFlag<string>;
        remote: flags.IOptionFlag<string | undefined>;
    };
    static args: {
        name: string;
        optional: boolean;
        description: string;
    }[];
    app: string;
    run(): Promise<void>;
    private fetchLatestConfig();
    private diffPrompt(original, newConfig);
    private verifyUnchanged(original);
    private updateConfig(original, newConfig);
}
